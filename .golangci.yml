version: "2"

linters:
  default: standard

  enable:
    - gocyclo

  settings:
    gocyclo:
      min-complexity: 15

  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$

    presets:
      - common-false-positives
